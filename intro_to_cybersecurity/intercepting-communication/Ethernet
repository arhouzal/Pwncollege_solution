import socket

# Création d'un socket brut de couche 2 lié à eth0
s = socket.socket(socket.AF_PACKET, socket.SOCK_RAW)
s.bind(("eth0", 0))

# En-tête Ethernet (14 octets)
dst_mac = b'\xff\xff\xff\xff\xff\xff'
src_mac = bytes.fromhex("668e3eee58be")
eth_type = b'\xff\xff'

# Assemblage de la trame et de la charge utile
payload = b"hello challenge"
trame_brute = dst_mac + src_mac + eth_type + payload

# Envoi direct sur le réseau
s.send(trame_brute)
print("[+] Trame brute envoyée avec succès via socket natif !")
