from scapy.all import *
# creation de tcp packet avec les parametre du challenge 
tcp_packet = IP(dst = "10.0.0.2") / TCP(sport=31337, dport=31337,seq=31337, ack=31337, flags="APRSF")
# envoie de packet dans le resaux sans attender la reponse
send(tcp_packet)

